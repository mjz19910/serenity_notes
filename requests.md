```cpp
ECMAScriptFunctionObject::prepare_for_ordinary_call
```
```log
WebContent(49:49): Unrecognized CSS at-rule: @keyframes

```
```css
body {
	image-rendering:0;
	font-smooth:0;
	content:0;
	animation:0;
}
```
```cpp
//handle invalid cases
JS::Bytecode::Op::Call
Web::HTML::HTMLParser::the_end (){
	// don't spin until, the call_stack will continue
	// to grow, this might not be what we want.
	// save the html page we are waiting for,
	// then run the callback when we hit that event
	Web::HTML::EventLoop::spin_until()
}
```
```js
element.setAttribute("on"+'listener','code') && element.attributes["on"+'listener']
```